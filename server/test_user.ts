import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findFirst({ where: { email: 'akhilp_manager@gmail.com' }, include: { role: true } }); console.log('ROLE:', user?.role); } main();
